<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/color_background">

    <!-- Passport Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@color/color_primary"
        android:padding="20dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/label_passport_title"
            android:textColor="@color/color_on_primary"
            android:textSize="20sp"
            android:textStyle="bold"
            android:fontFamily="@font/noto_serif" />

        <!-- Badge count row -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="8dp"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/text_badge_count"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:text="0"
                android:textColor="@color/color_primary"
                android:textSize="16sp"
                android:textStyle="bold"
                android:fontFamily="@font/noto_serif"
                android:gravity="center"
                android:background="@drawable/bg_stamp_badge" />

            <TextView
                android:id="@+id/text_stamps_label"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="0 stamps collected"
                android:textColor="@color/color_secondary_light"
                android:textSize="13sp"
                android:fontFamily="@font/noto_serif"
                android:layout_marginStart="10dp" />

        </LinearLayout>

        <!-- Lotus divider -->
        <View
            android:layout_width="match_parent"
            android:layout_height="2dp"
            android:background="@drawable/divider_lotus"
            android:layout_marginTop="12dp" />

    </LinearLayout>

    <!-- Empty state -->
    <LinearLayout
        android:id="@+id/layout_empty_passport"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="40dp"
        android:visibility="visible">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🏛"
            android:textSize="64sp"
            android:layout_marginBottom="16dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/label_passport_empty_title"
            android:textColor="@color/color_primary"
            android:textSize="18sp"
            android:textStyle="bold"
            android:fontFamily="@font/noto_serif"
            android:gravity="center" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/label_passport_empty_subtitle"
            android:textColor="@color/color_text_secondary"
            android:textSize="14sp"
            android:fontFamily="@font/noto_serif"
            android:gravity="center"
            android:lineSpacingMultiplier="1.5"
            android:layout_marginTop="8dp" />

    </LinearLayout>

    <!-- Stamp list -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_passport"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="12dp"
        android:clipToPadding="false"
        android:visibility="gone" />

</LinearLayout>
