package com.virasat.nammaguide.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand Palette — Indian Temple Architecture ──────────────────────────────
val TempleMaroon      = Color(0xFF8B1A1A)   // deep temple maroon (primary)
val TempleMaroonDark  = Color(0xFF6B1010)   // darker maroon
val SandstoneGold     = Color(0xFFC8860A)   // sandstone gold (secondary)
val SandstoneGoldLight= Color(0xFFE8A020)   // lighter gold
val AgedParchment     = Color(0xFFFDF3E3)   // aged parchment / ivory (background)
val WarmOffWhite      = Color(0xFFFFF8F0)   // warm off-white (surface)
val WarmBrown         = Color(0xFF2C1810)   // dark warm brown (on-background text)
val MidBrown          = Color(0xFF7B4F35)   // secondary text
val LegendCard        = Color(0xFFFFF0D0)   // legend card background
val StampMaroon       = Color(0xFF8B1A1A)   // stamp badge

// ── Site thumbnail placeholder colors (warm earth tones) ────────────────────
val SiteColor1 = Color(0xFF8B1A1A)   // Hampi
val SiteColor2 = Color(0xFF5C3317)   // Belur
val SiteColor3 = Color(0xFF4A3728)   // Badami
val SiteColor4 = Color(0xFF2E4A3E)   // Shravanabelagola
val SiteColor5 = Color(0xFF6B4226)   // Aihole
